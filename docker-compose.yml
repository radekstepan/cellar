services:
  app:
    build: .
    ports:
      - "3001:3001"
    volumes:
      - ./data/records:/app/data/records
      - ./data/schema.json:/app/data/schema.json
    environment:
      - NODE_ENV=production
